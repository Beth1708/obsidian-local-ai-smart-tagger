# Dependencies
node_modules/

# Local compilation output (handled by your build tool)
# We want to ignore the temp build folder, but keep main.js in the root
.esbuild/

# OS-specific clutter
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*